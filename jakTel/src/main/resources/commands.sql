create table customer_jakTel(
phone_no bigint primary key,
name varchar(50),
age integer,
gender char(10),
address varchar(50),
plan_id integer
);


select * from customer_jaktel;

TRUNCATE TABLE customer_jaktel;
